# Configuración de npm para Vercel
# Asegurar que las dependencias opcionales se instalen correctamente
# Esto soluciona el problema con módulos nativos en Vercel (rollup, lightningcss)
optional=true
legacy-peer-deps=false
fund=false
audit=false